<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      padding: 2rem;
    }
    h1 { color: #2c3e50; border-bottom: 2px solid #3498db; padding-bottom: 0.5rem; }
    .day { margin-bottom: 2rem; }
    .section { margin-left: 1rem; }
    .activity { margin-left: 1.5rem; color: #7f8c8d; }
  </style>
</head>
<body>
  <h1 class="pdf-ready"><%= destination %> Trip Plan</h1>
  
  <% tripPlan.forEach(day => { %>
    <div class="day">
      <h2>Day <%= day.day %></h2>
      <% day.sections.forEach(section => { %>
        <div class="section">
          <h3><%= section.name %></h3>
          <div class="activity"><%= section.activities %></div>
        </div>
      <% }); %>
    </div>
  <% }); %>

  <footer style="margin-top: 3rem; text-align: center; color: #95a5a6;">
    Generated by TripAI • <%= new Date().toLocaleDateString() %>
  </footer>
</body>
</html>